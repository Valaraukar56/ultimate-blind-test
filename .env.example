# Variables lues par docker-compose (service server).
# Copie ce fichier en .env sur le VPS : cp .env.example .env

# Origine CORS autorisée. "*" convient en same-origin derrière ton Caddy.
CLIENT_URL=*

# Réglages de jeu (optionnels)
ROUNDS_PER_GAME=10
ROUND_DURATION_SEC=30
